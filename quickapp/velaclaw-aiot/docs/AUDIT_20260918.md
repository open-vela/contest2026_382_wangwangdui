# 技术审计 · 2026-09-18

本轮审计覆盖启动器入口、构建体积、图标一致性、文档链接与两个 Quick App 的完整质量门禁。所有结论都来自本次实际执行的命令输出。

## 1. 质量门禁

| 检查 | 命令 | 结果 |
|---|---|---|
| 主工程完整门禁 | `npm run check` | 29/29 步通过 |
| 主工程 lint（源码） | `node scripts/lint.js` | 268 文件，0 errors / 0 warnings |
| 设计工具 lint | `node scripts/lint.js tools/design-studio` | 14 文件，0 errors / 0 warnings |
| 设计工具自测 | `node tools/design-studio/test/design_studio.test.js` | 21/21 通过 |
| 番茄钟逻辑用例 | `npm test`（quickapp/pomodoro） | 35 条用例通过（timer_machine 10 / timer_layout 11 / settings 5 / stats 5 / date_key 4） |
| 番茄钟静态审计 | `audit-quickapp.mjs` | No static guardrail findings |
| 文档链接 | `node scripts/check-docs.js` | 18 个 Markdown，0 断链 |

`npm run check` 现在共 29 步，其中本轮新增 `registry:check`。

## 2. 体积审计

设备安装器拒绝包含单文件 > 1 MiB 的包，因此有两道闸门：

| 检查 | 范围 | 结果 |
|---|---|---|
| 页面包预算 | `build/pages/**/*.{js,jsc}`，硬上限 1024 KiB，禁止内联 source map | 通过，最大 `clock.js` 655.4 KiB |
| 产物体积 | `build/` 全部文件 + 发布 RPK 内条目 | 通过，最大条目 113.4 KiB（`pages/clock/clock.jsc`） |

干净构建（删除 `build/`、`dist/` 后重建）：

```text
RPK  dist/com.application.watch.demo.debug.2.4.0.rpk   570,928 B
最大打包条目                                          113.4 KiB
```

内联 source map 的根因与归一位置见根目录 README 的「安装体积上限」一节：构建路径由 `quickapp.config.js` 统一，IDE 与命令行都读它。

## 3. 应用注册表审计

新增 `scripts/check-app-registry.js`，把「启动器条目 → 图标 → 路由 → 页面文件 → manifest」整条链和反向的「页面文件是否可达」都走一遍：

```text
launcher surfaces: base, circle, pill, rect
  每个形态 13 个应用
distinct placed apps: 13
manifest pages: 21
```

结论：13 个上线应用全部解析到图标对（主图标 + 柔化图标）、路由、页面文件与 manifest 条目。

已知告警（历史行为，非断链）：`clock` 没有独立路由，点按只做返回。

## 4. 图标一致性

```text
hard icons: 15 | soft icons: 15 | issues: none
```

全部为 96×96 JPEG，每个主图标都有对应的柔化版本，无孤立文件。各图标强调色：

| 图标 | 强调色 | 图标 | 强调色 |
|---|---|---|---|
| brightness | #FDD70D | motion | #AE53DD |
| calendar | #F43D61 | notification | #FDA00C |
| clock | #0B85FE | pomodoro | #F85D39 |
| diagnostics | #33AEE5 | settings | #908F93 |
| faces | #01C7BD | sync | #167EF8 |
| health | #32AEE6 | vibration | #BB60EA |
| heart | #F83A60 | workout | #2ED259 |
| history | #5E5BE4 | | |

番茄钟图标与内建图标同构（实心强调色圆 + 白色字形 + 淡内描边），由 `assets/icons/pomodoro.svg` 经项目既有渲染链生成，量化比对见 README「番茄钟入口」一节。

## 5. 启动器几何

13 个应用在两种屏形态下的解析结果：

```text
rect 336x480   | 13 项 | 13 个唯一格位 | 静止可见 13 | 首项 pomodoro
circle 466x466 | 13 项 | 13 个唯一格位 | 静止可见 13 | 首项 pomodoro
pill 212x520   | 分页列表，首页 pomodoro / workout / history / heart
```

点击解析：13 个可见格各自的中心点都解析回自身；首位番茄钟格在中心与偏离中心 40% 处都能命中；空白区域不命中。滑动浏览与点击互不干扰（点击只在未拖动分支处理）。

## 6. 设备侧验证

在 `emulator-5554`（Vela_Virtual_Device，336×480 矩形手环）上：

| 项 | 结果 |
|---|---|
| `pm install` | 成功 |
| `pages/pomodoro/pomodoro.jsc` | 14,640 B |
| `common/icons/pomodoro.jpg` | 4,721 B |
| `common/icons/soft/pomodoro.jpg` | 2,193 B |
| `manifest.router.pages["pages/pomodoro"]` | 存在 |

## 7. 未覆盖项

- 启动器与番茄钟页面的**像素级**截图确认：Vela（NuttX）镜像不提供 `screencap`，`/bin` 中亦无帧缓冲抓取工具，宿主窗口抓取未获得设备画面。相关工具留在仓库 `tools/`（`emu_window.py`、`emu_capture.py`、`png_check.py`）供后续取证。
- 系统启动器（系统镜像自带）为何不显示侧载的 `com.application.pomodoro`：不在本仓库控制范围内，已改为在应用内提供等效入口。
